name: backend
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.3
  shelf_io: ^1.1.0
  shelf_cors_headers: ^0.2.0
  firebase_admin: ^0.2.0   # Firebase Admin SDK for Dart（サーバー用）
  mailer: ^6.0.0
  crypto: ^3.0.3
  googleapis: any
  googleapis_auth: any

