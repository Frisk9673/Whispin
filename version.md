# Flutter Firebase パッケージ更新手順

## 目的

以下の Firebase / Flutter パッケージを **指定バージョンまで更新** し、
アプリが正常に起動することを確認する。

---

## 目標バージョン

```yaml
firebase_core: 4.2.1
firebase_auth: 6.1.2
cloud_firestore: 6.1.0
image_picker: 1.2.1
```

上記バージョンになっていれば OK。

---

## 手順

### 1. メジャーバージョンを含めてアップグレード

```bash
flutter pub upgrade --major-versions
```

---

### 2. ビルドキャッシュの削除

```bash
flutter clean
```

---

### 3. 依存関係の再取得

```bash
flutter pub get
```

---

### 4. アプリ起動確認

```bash
flutter run
```

👉 **正常に起動できれば OK**

---

## 環境情報（確認用）

```bash
flutter --version
```

```text
Flutter: 3.38.1
```

```bash
dart --version
```

```text
Dart SDK version: 3.10.0
```

---

## チェックリスト

* [ ] `pubspec.lock` の各パッケージが指定バージョンになっている
* [ ] `flutter run` がエラーなく完了する
* [ ] Firebase 初期化エラーが出ない

---

## 補足

* 依存関係エラーが出る場合は `flutter pub outdated` で競合を確認
* iOS の場合は `ios/Podfile` と `pod install` の再実行も検討
